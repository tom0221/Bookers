<h1>New book</h1>

<%= form_for(@book,url:'/books') do |f| %>
<%= render 'layouts/error_message', model: f.object %>
   <h4>Title</h4>
   <%= f.text_field :title %>

   <h4>Body</h4>
   <%= f.text_area :body %>

   <%=f.submit 'Create Book' %>

<% end %>